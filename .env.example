# Server
PORT=8787
CORS_ORIGIN=*

# Password reset
# Token expiry in minutes (recommended 15–60)
RESET_TTL_MIN=30
# DEV ONLY: return resetToken in API response when SMTP isn't configured
RESET_DEBUG=0
JWT_SECRET=change_me_long_random
STORAGE_DRIVER=json

# JSON storage (dev)
JSON_DB_PATH=./data/db.json

# Frontend base URL (used for links in reset emails)
FRONTEND_URL=http://localhost:5500

# SMTP (Mailtrap) - fill with your Mailtrap SMTP credentials
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_SECURE=false
SMTP_USER=YOUR_MAILTRAP_USER
SMTP_PASS=YOUR_MAILTRAP_PASS

MAIL_FROM="MKO.pl <no-reply@mko.test>"

# Helpers
DEV_BETA=true
